<%= form_with(model: book) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
<% end %>


<%= form_with model: @book do |form| %>
  <%= form.text_field :title, class:'form-control', placeholder: 'Title' %><br>
  <%= form.text_area :author,class:'form-control', placeholder: 'Author' %><br><br>
  <%= form.select :status,[["En estante", :shelf], ["Prestado", :lent]],class:'form-control' %><br><br>
  <%= form.label :checkout%>
  <%= form.date_select :checkout, class:'form-control' %><br><br>
  <%= form.label :checkin%>
  <%= form.date_select :checkin, class:'form-control' %><br><br>
  <br><br>
  <div class = "text-center">
  <%= form.submit 'Enviar', class: 'btn btn-primary' %>
  </div><br>
<% end %>
